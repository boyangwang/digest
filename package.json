{
  "name": "digest",
  "version": "1.2.0",
  "description": "Digest — Telegram bot that compiles multimodal inputs into a single bilingual Obsidian note (dear diary).",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.js",
    "retranscribe": "node scripts/retranscribe.mjs"
  },
  "dependencies": {
    "grammy": "^1.30.0",
    "openai": "^4.73.0",
    "yaml": "^2.6.0"
  }
}
